import React, { FC }  from "react";
import { SkillsPageWrapper } from "./SkillsPageStyle";

export const SkillsPage: FC = () => {
    return(
        <SkillsPageWrapper id="skills">
            Strona 3 - Umiejętności
        </SkillsPageWrapper>
    );
};